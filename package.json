{
  "name": "stack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "create-stack": "yarn ts-node scripts/create-stack",
    "update-stack": "yarn ts-node scripts/update-stack",
    "list-buckets": "yarn ts-node scripts/list-buckets"
  },
  "dependencies": {
    "@types/node": "^9.6.4",
    "aws-sdk": "^2.224.1",
    "ts-node": "^5.0.1",
    "typescript": "^2.8.1"
  }
}
